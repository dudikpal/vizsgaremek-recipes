create table ingredients (id bigint auto_increment,
                          name varchar(255),
                          type varchar(20),
                          quantity decimal,
                          primary key (id));